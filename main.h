#ifndef HEADER_H
#define HEADER_H

/* C standard library header files */
#include <stdarg.h>
#include <unistd.h>

/* function prototypes */
int putchr(char z);
int _printf(const char *format, ...);
int put_s(char *string);

#endif /* HEADER_H */
